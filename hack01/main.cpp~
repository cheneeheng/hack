#include<iostream>
#include<cmath>
#include<cstdio>
#include<vector>
#include<algorithm>
#include<sstream>
#include<string>
using namespace std;

int main(int argc, char *argv[])
{
	int num,num2;
	double *arrays;

	string inputs;

	getline(cin,inputs);

	stringstream parse(inputs);
	
	parse >> num;
parse >> num;

	cout << num;

	return 0;	
}
